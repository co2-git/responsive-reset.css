{
  "name": "responsive-reset.css",
  "version": "0.0.2",
  "description": "Stylesheet that does a reset and set typo for responsive CSS.",
  "main": "build.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "babel": "babel --out-dir dist app",
    "babel-watch": "babel --watch --out-dir dist app",
    "build": "node dist/build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-preset-es2015": "^6.5.0",
    "babel-preset-stage-0": "^6.5.0",
    "cassis": "^1.0.2"
  }
}
